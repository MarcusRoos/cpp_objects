#include <HousingQ.h>


using std::cout;
using std::endl;
/**
Function assignmentFunction(), the main program which will create an object
 of HousingQ and call it "house", this object will call the function called
 run(), nothing more is done within this function, it's simply there as a
 means to start the program.
*/
void assignmentFunction() {
    HousingQ house;
    house.run();
}
